"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import {getSupabaseBrowserClient} from "@/lib/supabase-client";
export default function Register(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[msg,setMsg]=useState("");
async function submit(e:FormEvent){e.preventDefault();setMsg("");const {data,error}=await getSupabaseBrowserClient().auth.signUp({email,password,options:{emailRedirectTo:location.origin+"/account"}});if(error){setMsg(error.message);return}if(data.session)location.href="/account";else setMsg("Account created. Check your email to confirm your address, then sign in.");}
return <main className="grid min-h-screen place-items-center bg-[#f7f9f4] p-5"><form onSubmit={submit} className="w-full max-w-md rounded-[30px] bg-white p-8 shadow-sm"><Link href="/" className="text-2xl font-bold">CVUp</Link><h1 className="mt-8 text-3xl font-bold">Create your account</h1><p className="mt-2 text-slate-500">Save your information and reuse it for future CVs.</p><label className="mt-6 block">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border p-3"/></label><label className="mt-4 block">Password<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border p-3"/></label>{msg&&<p className="mt-3 text-sm text-slate-600">{msg}</p>}<button className="mt-6 w-full rounded-full bg-[#102019] p-3 font-bold text-white">Create account</button><p className="mt-5 text-center text-sm">Already registered? <Link className="underline" href="/account/login">Sign in</Link></p></form></main>}